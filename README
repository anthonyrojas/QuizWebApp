#Quiz Web App

A simple quiz website written using the LAMP stack. The master branch contains the project being done with pure custom javascript, CSS, and PHP. The materialize branch will contain the site done using MaterializeCSS. 